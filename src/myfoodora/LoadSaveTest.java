package myfoodora;

import static org.junit.Assert.*;

import org.junit.Test;

public class LoadSaveTest {
	
	/*
	@Test
	public void testLoadSave(){
		//TODO : uncomment loadsys and savesys (commentés pour ne pas effacer les donnes par erreur)
		if(true){
			MyFoodora sys = new MyFoodora();
			Customer antho = new Customer("antho", "Antho", "Gauv", new Coordinates(3, 3), "aa", sys);
			Restaurant ru = new Restaurant("ru", "Restaurant Universitaire", new Coordinates(3,3), "aa", sys);
			Courier jodev = new Courier("jodev", "Joseph", "Dev", 000111222, sys);
			
			//LoadSave.saveSys(sys);
		}
		
		//MyFoodora sys2 = LoadSave.loadSys();
		
		assertEquals(sys2.getCustomers().get(0).getUsername(), "antho");
		assertEquals(sys2.getRestaurants().get(0).getUsername(), "ru");
	}
	*/
}
